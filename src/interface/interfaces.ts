export interface RepoInfo {
  name: string;
  description: string;
  stargazerCount: number;
  forkCount: number;
  primaryLanguage: {
    name: string;
  };
  updatedAt: string;
  licenseInfo: {
    name: string;
  };
}
